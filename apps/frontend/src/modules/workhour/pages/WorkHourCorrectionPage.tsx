import { PageContainer } from '../../../components/common/PageContainer';
import { EmptyState } from '../../../components/common/EmptyState';

export function WorkHourCorrectionPage() {
  return (
    <PageContainer title="工时修正">
      <EmptyState description="工时修正页面骨架已就绪（含审计日志）" />
    </PageContainer>
  );
}
